import { shallowMount } from '@vue/test-utils'
import App from '../App.vue'

describe('App.vue', () => {
  describe('parseMinutes', () => {
    it('does a thing', () => {
      return true
    })
  })
})
